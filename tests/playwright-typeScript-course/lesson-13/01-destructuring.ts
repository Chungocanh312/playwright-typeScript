const myClass = {
  school: "BBA",
  course: "Full-stack QA",
};

const { school, course } = myClass;

console.log(school);
console.log(course);

const student = {
  nameStudent: "Anthony",
  address: "Vietnam",
};

const { nameStudent, address } = student;

console.log(nameStudent);
console.log(address);
